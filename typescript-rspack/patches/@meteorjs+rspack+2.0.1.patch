diff --git a/node_modules/@meteorjs/rspack/lib/test.js b/node_modules/@meteorjs/rspack/lib/test.js
index d72e4bb..e9eb374 100644
--- a/node_modules/@meteorjs/rspack/lib/test.js
+++ b/node_modules/@meteorjs/rspack/lib/test.js
@@ -74,14 +74,17 @@ const generateEagerTestFile = ({
     exclude: ${excludeFoldersRegex.toString()},
     mode: 'eager',
   });
-  ctx.keys().filter((k) => {
+  // fix(meteor#14371): await async webpackContext imports so test files
+  // that transitively depend on async modules are fully evaluated before
+  // mocha runs.
+  await Promise.all(ctx.keys().filter((k) => {
     ${
       excludeMeteorIgnoreRegex
         ? `// Only exclude based on *relative* path segments.
     return !MeteorIgnoreRegex.test(k);`
         : "return true;"
     }
-  }).forEach(ctx);
+  }).map(ctx));
   ${
     extraEntry
       ? `const extra = import.meta.webpackContext('${toPosix(path.dirname(
@@ -91,7 +94,7 @@ const generateEagerTestFile = ({
     regExp: ${new RegExp(`${path.basename(extraEntry)}$`).toString()},
     mode: 'eager',
   });
-  extra.keys().forEach(extra);`
+  await Promise.all(extra.keys().map(extra));`
       : ""
   }
 }`;
